src/routeTree.gen.ts
*.toml

# shadcn/ui primitives are vendored (generated by the shadcn CLI). Keep them
# pristine — never auto-format them, so they stay diffable against upstream.
# ESLint also ignores this dir (see globalIgnores in eslint.config.ts).
src/components/ui/

# Supabase Edge Functions run under Deno and are formatted by `deno fmt`
# (100-col), which conflicts with Prettier's 80-col wrapping. Let Deno own
# their formatting so the Deno CI fmt check stays green.
supabase/functions/

# seed.sql is a generated pg_dump of the demo world; not hand-maintained.
supabase/seed.sql

# seed regeneration tooling is hand-formatted dev scripting.
supabase/seed_tools/
