VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# SMTP Configuration for magic-link email delivery
# For local development, use Inbucket (running on localhost:54324)
# The Supabase local dev server automatically includes Inbucket
SUPABASE_SMTP_HOST=localhost
SUPABASE_SMTP_PORT=54324
# Optional: leave empty for Inbucket (no auth required)
SUPABASE_SMTP_USER=
SUPABASE_SMTP_PASS=
# Admin email sender address (shown as "From" in sent emails)
SUPABASE_SMTP_ADMIN_EMAIL=noreply@gubernator.local
# Human-friendly sender name
SUPABASE_SMTP_SENDER_NAME=Gubernator
