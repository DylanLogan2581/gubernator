// Re-exports the single source of truth at src/shared/education so the
// edge simulation and the browser UI can never drift on education rules.
export * from "../../../../src/shared/education/index.ts";
