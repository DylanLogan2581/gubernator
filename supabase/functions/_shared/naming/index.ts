// Re-exports the single source of truth at src/shared/naming so the edge
// simulation and the browser UI can never drift on name-generation rules.
export * from "../../../../src/shared/naming/index.ts";
