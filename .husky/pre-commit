npx lint-staged

# Everything else (typecheck, affected tests, edge/db checks) lives in the
# commit-msg hook so commitlint can reject a bad message in seconds BEFORE
# the expensive checks run. Git runs pre-commit first, then commit-msg —
# keeping only fast, staged-file mutations (lint-staged) here means a
# rejected message never costs a full typecheck re-run.
